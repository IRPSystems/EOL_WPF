<Window x:Class="EOL.Views.DBTerminalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EOL.Views"
        mc:Ignorable="d"
        Title="DBTerminalView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="2">
            <TextBox x:Name="DBconnectionTermianl" Text="{Binding DBconnectionString, UpdateSourceTrigger=PropertyChanged}"
                         HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Margin="10"
                        Visibility="Visible"
                        IsReadOnly="True"
                        AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto"
                        TextWrapping="Wrap"
                        FontFamily="Consolas"
                        FontSize="12"
                        BorderThickness="2"
                        BorderBrush="Black"
                        Background="GhostWhite" 
                        Foreground="Black"/>
        </Grid>
        <Button Grid.Column="0" Grid.Row="4" Height="30" Width="100"
                            Content="Clear" 
                            Command="{Binding ClearTerminalCommand}" />
        
    </Grid>
</Window>
